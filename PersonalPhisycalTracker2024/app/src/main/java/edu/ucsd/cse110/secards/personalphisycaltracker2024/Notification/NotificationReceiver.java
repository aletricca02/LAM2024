/*package edu.ucsd.cse110.secards.personalphisycaltracker2024.Notification;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;

//classe che sarà responsabile di inviare la notifica quando l'allarme viene attivato.

public class NotificationReceiver extends BroadcastReceiver {

    @Override
    public void onReceive(Context context, Intent intent) {
        NotificationHelper notificationHelper = new NotificationHelper(context);
        notificationHelper.sendNotification("Hurry Up!", "It is time to track some activity! Let's go!");
    }
}*/