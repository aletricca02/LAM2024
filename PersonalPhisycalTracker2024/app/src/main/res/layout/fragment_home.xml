<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#f5f5f5"
    android:padding="16dp">
    <!-- Title -->
    <TextView
        android:id="@+id/titleTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Personal Physical Trainer"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="#000080"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="20dp"/>

    <!-- New Text between Title and Circle -->
    <TextView
        android:id="@+id/betweenTitleAndCircleText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Inizia a registrare la tua attività!"
        android:textSize="18sp"
        android:textStyle="italic"
        android:textColor="#000080"
        android:layout_below="@id/titleTextView"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="20dp"/>

    <!-- Circle with Timer -->
    <RelativeLayout
        android:id="@+id/circleLayout"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:background="@drawable/circle_shape"
        android:layout_below="@id/betweenTitleAndCircleText"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="20dp">

        <TextView
            android:id="@+id/timerTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="00:00:00"
            android:textSize="32sp"
            android:textStyle="bold"
            android:textColor="@color/DarkBlue"
            android:layout_centerInParent="true"/>
    </RelativeLayout>

    <!-- Activity Spinner -->
    <Spinner
        android:id="@+id/activitySpinner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/circleLayout"
        android:layout_marginTop="45dp"
        android:gravity="center"
        android:background="@drawable/spinner_background"
        android:popupBackground="@color/DarkBlue"
        android:popupElevation="@dimen/nav_header_vertical_spacing"
        android:padding="10dp"
        android:prompt="@string/choose_activity_prompt" />

    <!-- Activity Buttons -->
    <LinearLayout
        android:id="@+id/buttonLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/activitySpinner"
        android:layout_marginTop="57dp"
        android:gravity="center"
        android:orientation="horizontal">

        <Button
            android:id="@+id/startButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="10dp"
            android:background="@drawable/button_start"
            android:text="Start"
            android:textColor="#fff" />

        <Button
            android:id="@+id/stopButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/button_stop"
            android:text="Stop"
            android:textColor="#fff" />
    </LinearLayout>


    <!-- Activities List -->

</RelativeLayout>